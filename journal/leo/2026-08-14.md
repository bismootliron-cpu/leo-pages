# יומן leo — 2026-08-14

## מה בוצע היום

- נוספה לוגיקה ל-webhook handler של Whop ב-`main.py` — upsert ל-`singularity_customers` והודעת Telegram לאדמין כשמגיע אירוע `membership.activated` עם סטטוס `trialing`
- נוסף endpoint חדש `POST /singularity/questionnaire` — ולאחר מכן הוחלף ב-`/singularity/questionnaire/submit` עם שדות מדויקים יותר
- בוצעו שלוש ניסיונות לתקן את ה-endpoint — הניסיון השלישי כלל אזהרות מפורשות לגבי שמירת underscores

## מה ים יצר היום

לא בוצעו משימות יצירתיות (`__CREATIVE_TASK__`) היום — אין תמונות, וידאו, או מדיה שנוצרו.

## מה נלמד

- יש באג מערכתי אמיתי: underscores בתוך שמות מזהים (כמו `singularity_customers`, `tradingview_username`) נמחקים בשלב כלשהו לפני שההוראות מגיעות לביצוע — כנראה בגלל `parse_mode="Markdown"` ב-Telegram שמפרש underscores כסימוני italic
- אני לא יכול לאבחן את הבעיה בעצמי — אין לי יכולת לקרוא קבצים או לדפדף ב-repo, רק לכתוב דרך `__REPO_EDIT__`
- שלוש איטרציות על אותו endpoint זה יקר ומבזבז — היה צריך לאתר את הבאג המערכתי לפני הניסיון הראשון

## מה אפשר לשפר

- הבאג של ה-underscores חייב תיקון מערכתי לפני כל עבודה נוספת — כל עוד ההוראות עוברות דרך Markdown parsing, שמות עם underscores יישברו שוב
- כשניסיון ראשון נכשל, לדרוש אבחון לפני ניסיון שני — לא לנסות שוב עם אותה תשתית שבורה
- לבקש confirmation מהמשתמש שה-commit בפועל נראה נכון לפני שממשיכים למשימה הבאה
