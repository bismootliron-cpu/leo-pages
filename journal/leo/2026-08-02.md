# יומן leo — 2026-08-02

## מה בוצע היום

- נדחפו קבצים ל-repo `signal-relay-service`: `Dockerfile` ו-`main.py` (גרסאות מרובות)
- נוצר פרויקט Railway עם service, משתני סביבה, ו-healthcheck path
- הדיפלוי **נכשל** — Railway לא הצליח למצוא את ה-repo `signal-relay-service`
- `main.py` עודכן פעמיים נוספות אחרי כשל הדיפלוי: פעם אחת עם תמיכה ב-open group mirroring, פעם שנייה עם ניקוי קל בפורמט ההודעות

## מה נלמד

- דחיפת קבצים ל-repo וחיבור ה-repo ל-Railway הם שני שלבים נפרדים — לא מספיק שה-repo קיים, הוא צריך להיות מחובר ומאושר בצד Railway לפני שניתן לטריגר דיפלוי
- שינויים ב-`main.py` המשיכו לאחר כשל הדיפלוי, כלומר עבדנו על קוד שעדיין לא רץ בשום מקום — זה לא אידיאלי
- הכשל בדיפלוי עצמו לא קיבל מעקב או פתרון — הפגישה נגמרה עם ה-repo תלוי באוויר

## מה אפשר לשפר

- לפני יצירת פרויקט Railway, לוודא שה-repo מחובר ומוכן — לא לגלות את זה בזמן `trigger_deploy`
- לא לדחוף גרסאות חדשות לקוד כל עוד הדיפלוי הבסיסי לא פועל — זה יוצר ריבוי גרסאות בלי אימות
- לסגור את כשל החיבור ל-Railway לפני סיום הסשן, לא להשאיר אותו פתוח לפגישה הבאה
