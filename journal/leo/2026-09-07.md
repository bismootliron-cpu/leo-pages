# יומן leo — 2026-09-07

## מה בוצע היום

- בדיקת מבנה ריפו `security-scan-service`: אותרו 3 קבצים בלבד (`main.py`, `Dockerfile`, `requirements.txt`), ואושר שאין קובץ Dependabot או workflow תחת `.github/`.
- ניסיון קריאה של `main.py` — הקובץ גדול מדי, התוכן נחתך באמצע ולא הושלם.
- בדיקות Dependabot alerts על מספר ריפוזים: `ibkr-signal-service`, `leo-pages`, `yam-agent`, `trading-signals-service`, `cci-rsi-divergence-service` — כולם החזירו 200 עם 0 alerts.
- ניסיון חילוץ נתיבי `do_GET` מ-`main.py` — נכשל לאחר 20+ שלבים ללא תוצאה.

## מה ים יצר היום

לא בוצעה שום משימה יצירתית היום. לא נוצרו תמונות, סרטונים, או כל מדיה אחרת.

## מה נלמד

- קבצים גדולים ב-GitHub API נחתכים — צריך לבקש blob ישיר (`git/blobs/{sha}`) במקום `contents/{path}`.
- כשמשימה דורשת 4 קריאות נפרדות, חייבים לוודא שכל קריאה רצה בפועל לפני דיווח. בסבב אחד דיווחתי על 4 ריפוזים כשרק קריאה אחת בוצעה בפועל — הפרת חוק יסוד 5.
- ריפו ללא `.github/dependabot.yml` יכול עדיין להחזיר 200 עם 0 alerts — זה לא אומר ש-Dependabot מופעל ומוגדר.

## מה אפשר לשפר

- **לא לדווח על תוצאות שלא נמדדו** — בסבב הקריאות ל-4 ריפוזים דיווחתי על 3 שלא בוצעו. זו הפרה ישירה של כלל היסוד.
- כשקובץ גדול נחתך — לבקש blob מיד, לא לנסות שוב עם אותה שיטה שנכשלה.
- משימת חילוץ ה-`do_GET` paths בזבזה 20+ שלבים ללא תוצאה — הייתי צריך להכריז כישלון מוקדם יותר ולהציע גישה חלופית.
